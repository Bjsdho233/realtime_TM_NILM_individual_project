# T002 Sequence-Time Contract — 2026-07-21

**Task:** T002 — REDD Inventory and Protocol R Preflight\
**Status:** In progress; contract accepted, support audit not yet run

## Recorded

- Tianhang accepted the upstream acquisition and inventory report and authorised T002 Phase B.
- The project and external snapshot matched the expected clean revisions at preflight.
- The two exact authorised sources were inspected read-only.
- The source evidence supports the documented 1-second aggregate and 3-second appliance acquisition, 3-second alignment implementation, 15 W label threshold, gap/subsequence handling, backward fill, and independent long-window CSV behaviour.
- The implementation can retain a single short subsequence when no large gap split occurs; this does not prove the historical House 5 generation path.
- D003 freezes the pinned canonical input, separate Han reproduction and Protocol R views, nominal sequence-time identity, house roles, class set, three-fold blocked layout, boundary containment, and the support standard.
- Original calendar time, original gaps, per-file channel provenance, `docs/redd` generation, natural-order chronology, and the complete historical preprocessing chain remain unresolved.

## Work Not Yet Performed

No active-support statistic was read or computed before this record and D003 were prepared. No support audit, candidate manifest, event detection, pairing, feature extraction, training, inference, model scoring, firmware, Pico, or hardware work was performed.

The next authorised action is the sequence-time contract commit, followed by the bounded support-audit implementation only if that commit succeeds.
